import 'package:flutter/material.dart';

class AppColors {
  // Arkaplan renkleri
  static const Color scaffoldBg = Color(0xFF1A1A1A);
  static const Color cardBg = Color(0xFF2A2A2A);
  static const Color navbarBg = Color(0xFF1E1E1E);

  // Vurgu renkleri
  static const Color primary = Colors.deepPurpleAccent;

  static const Color secondary = Colors.purpleAccent;

  static const Color gold = Color(0xFFFFD700);

  // Durum renkleri
  static const Color success = Colors.greenAccent;
  static const Color warning = Colors.orange;
  static const Color error = Colors.redAccent;

  // Yazı renkleri
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.grey;
  static final Color textMuted = Colors.grey[400]!;

  // Özel renkler

  static final Color overlay = Colors.black.withOpacity(0.3);
}
